package PACKAGE_NAME;

public class Massive {

    private ArrayList<Double> values;
    private ArrayList<String> string;

    public Massive(){}


     public ArrayList<Double> values findSum(ArrayList<Double> values){
         Double sum =0;
         for (Double value:values){
          sum = sum+value;
         }
         return sum;
     }

    public ArrayList<Double> printArrayReverse(ArrayList<Double> values) {
        for (int i = list.size() - 1; i >= 0; i--) {
            ArrayList<Double> valuesReverse = list.get(i);
        }
        return values;
    }

      /// string

        public ArrayList<String> containsElement(ArrayList<String> string) {
            String str;
            for (String s : string) {
                if (str.equals(s)) {
                    return true;
                }
            }
            return false;
        }




}
